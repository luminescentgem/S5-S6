#ifndef CONFIG_H
#define CONFIG_H

#include <cstdint>
#include <cstddef>
#include <string>

const char* _resources_directory = "@RESOURCES_DIRECTORY@";	///< repertoire contenant les ressources.

std::string full_path(std::string uri) {
    return std::string(_resources_directory) + uri;
}

#endif
